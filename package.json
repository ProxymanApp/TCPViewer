{
  "name": "tcpviewer-release-tools",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "release": "node scripts/release.mjs",
    "release:homebrew": "node scripts/release.mjs --homebrew-only",
    "test": "node --test tests/**/*.test.mjs"
  },
  "dependencies": {
    "create-dmg": "^8.1.0",
    "prompts": "^2.4.2"
  }
}
